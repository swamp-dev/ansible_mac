# Productivity Tools - Brew packages are now installed via Brewfile (tasks/brew-bundle.yml)
# wireshark, libreoffice, flameshot, and xclip are in Brewfile

- name: Install sdkman
  ansible.builtin.command: 'curl -s "https://get.sdkman.io" | bash'
  tags:
    - mac-setup
    - productivity
