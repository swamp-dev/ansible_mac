- name: Install dust
  ansible.builtin.command: 'cargo install du-dust'
  tags:
    - cargo-packages

- name: Install cbfmt
  ansible.builtin.command: 'cargo install cbfmt'
  tags:
    - cargo-packages

- name: Install stylua
  ansible.builtin.command: 'cargo install stylua'
  tags:
    - cargo-packages

- name: Install cargo-update
  ansible.builtin.command: 'cargo install cargo-update'
  tags:
    - cargo-packages
