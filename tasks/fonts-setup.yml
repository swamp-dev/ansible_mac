# Fonts Setup - All fonts are now installed via Brewfile (tasks/brew-bundle.yml)
# The following fonts are installed via brew:
# - font-fantasque-sans-mono-nerd-font
# - font-fira-code-nerd-font
# - font-terminess-ttf-nerd-font
# - font-hack-nerd-font
# - font-inconsolata-go-nerd-font
# - font-source-code-pro-nerd-font
# - font-space-mono-nerd-font
# - font-noto-color-emoji
#
# Note: Fira Mono was previously installed via curl but has been removed.
# Fira Code (available via brew) is a similar alternative.

- name: Update font cache
  ansible.builtin.command: fc-cache -fv
  tags:
    - install
    - fonts
