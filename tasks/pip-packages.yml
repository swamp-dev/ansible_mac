- name: Install meson build system
  become: true
  ansible.builtin.pip:
    name: meson
  tags:
    - pip-packages

- name: Install ninja
  become: true
  ansible.builtin.pip:
    name: ninja
  tags:
    - pip-packages

- name: Install the AWS cli
  become: true
  ansible.builtin.pip:
    name: awscli
  tags:
    - pip-packages
